// Walkthrough test — DESHABILITADO durante Phase 1a v4.2.
//
// Este test depende del POOL_REGISTRY (pool de cartas), que en Phase 1a está
// con el schema v4.1 viejo. Phase 1b migra el pool a schema v4.2 y restaura
// este test ajustado al nuevo flow de juego.

import { describe, it } from 'vitest'

describe.skip('Walkthrough §11 — DISABLED durante Phase 1a v4.2', () => {
  it('placeholder hasta que Phase 1b migre el pool', () => {
    // ver scripts y SPEC v4.2 §9.2 para la migración del pool
  })
})
