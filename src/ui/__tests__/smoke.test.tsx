// UI smoke tests — DESHABILITADO durante Phase 1a v4.2.
// Phase 3 restaura cuando la UI esté adaptada al nuevo flow.

import { describe, it } from 'vitest'

describe.skip('UI smoke — DISABLED durante Phase 1a v4.2', () => {
  it('placeholder hasta Phase 3', () => {})
})
